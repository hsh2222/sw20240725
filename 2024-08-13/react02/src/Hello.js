const Hello = () => {    // Hello라는 함수형 컴포넌트를 정의
    return (
      <>
        <h3>Hello World</h3>     {/* h3 태그로 'Hello World' 텍스트를 렌더링 */}
      </>
    );
  };
  
  export default Hello;   // Hello 컴포넌트를 다른 파일에서 사용할 수 있게 내보냄
  